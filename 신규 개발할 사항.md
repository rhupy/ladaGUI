## 기능 요청 및 진행 상황

### v0.2.0 변경사항

1. [x] 작업 진행중에도 작업대기 리스트에 파일을 추가할수 있어야하고, 진행중이던 작업파일이라도 제거 → **파일 추가는 가능, 진행중 파일 제거 시 x 버튼 표시 (단, 진행중인 파일은 Docker 컨테이너 중지 필요하므로 현재는 pending/done/error만 제거 가능)**
2. [x] 프로그래스바 시간/속도 텍스트 흔들림 수정 → **`font-variant-numeric: tabular-nums` 적용 + 시간/속도를 프로그래스바 아래 우측정렬로 이동**
3. [x] 완료 후 PC 자동종료 옵션을 창 우측 하단으로 이동 → **footer 우측에 체크박스 배치**
4. [x] 진행 중에도 자동종료 체크박스 on/off 가능 → **footer의 체크박스는 항상 활성, 세팅과 양방향 동기화 (동일 변수 사용)**
5. [x] 잡 갯수: 숫자입력 + -/+ 버튼, 최대 99, 최소 1 → **number spinner UI 구현**
6. [x] 잡 갯수 우측에 ? 툴팁 (GPU별 추천 잡 수) → **tooltip-icon 구현, GPU별 추천치 표시**
7. [x] 다른 전문용어 옵션에도 툴팁 → **Detection Model, Max Clip Length, Encoder, CRF, Preset, Watermark 모두 툴팁 추가**
8. [x] 워터마크 옵션 UI → **체크박스 + 텍스트/크기/위치 설정 UI 추가 (후처리 ffmpeg 로직은 추후 구현)**
9. [x] 설정 저장 (앱 재시작 시 유지) → **Tauri app config dir에 settings.json 저장/로드**
10. [x] 다국어 설정 (영어/한국어) → **전체 UI 문자열 i18n 처리, 헤더에 EN/KO 토글 버튼**
11. [x] 성능탭 (CPU/GPU/VRAM) → **탭 UI 구현, 실시간 데이터는 향후 업데이트에서 연동**
12. [x] 로그탭 → **터미널 스타일 로그 뷰어, 에러 카운트 뱃지, localStorage 유지, 자동 스크롤**
13. [x] 디스코드 링크 → **헤더에 Discord SVG 아이콘 추가 (https://discord.gg/px4vBjTUBg)**
14. [x] 캔슬 후 잡 수 초과 버그 → **cancel_processing이 ALL lada 컨테이너를 stop하도록 수정**
15. [x] 일시정지 기능 → **docker pause/unpause 사용, 리소스 완전 반환됨, 이어서 작업 가능**

### 추가 구현사항
- [x] 스크롤바 디자인 개선 (커스텀 스크롤바 CSS)
- [x] Clear All 버튼 추가 (전체 목록 초기화)
- [ ] 워터마크 후처리 (ffmpeg drawtext) 실제 구현
- [ ] 성능탭 실시간 데이터 연동 (nvidia-smi / CPU 사용률 polling)
- [ ] 로그 자동 스크롤 최하단
- [ ] 작업 중 파일 추가 시 대기열 자동 반영 (현재는 다음 Start에서 반영)
- [ ] README에 디스코드 링크 추가

### 미해결
- 워터마크 후처리는 Lada 처리 완료 후 ffmpeg로 drawtext 필터 적용 필요 (처리 시간 +5~10%)
- 성능 모니터링은 nvidia-smi 파싱 또는 sysinfo crate 사용 필요
